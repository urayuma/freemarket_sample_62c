%body
  = render '/homes/header'
  - breadcrumb :in_progress
  = render "layouts/breadcrumbs"
  .main
    .mypage-container__listings

      .listings-product
        出品した商品

      = render 'listings', { url: @url }

    = render "sidebar"

  = render "/homes/footer"
